{
  "name": "pc-api",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "build": "go test -run '^$' ./...",
    "deploy": "echo 'Deploy via gcloud run or similar'",
    "test": "go test ./..."
  }
}
