{
  "name": "pc-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build required for pc-api Python service'",
    "deploy": "echo 'Deploy via gcloud run or similar'"
  }
}
